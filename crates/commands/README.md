# Crate `petunia_commands` (`crates/commands/`)

Implementa a máquina de histórico de operações e snapshots para comandos de Desfazer (`Undo`) e Refazer (`Redo`). Permite reversões exatas e transacionais de qualquer alteração estrutural ou de pintura realizada na malha.
