# Ponto de Entrada do Executável (`src/`)

Contém o ponto de entrada principal (`main.rs`) do binário executável `simple3d-modeling`.

Este arquivo atua como um wrapper fino que delega a execução diretamente para o coordenador de aplicação `petunia_app::run()`.
